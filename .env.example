# Optional local image tag. No .env is required for docker compose up --build.
VERSION=dev

# Only needed for compose.images.yml (pre-published backend images).
# IMAGE_REPOSITORY=your-dockerhub-user/your-repository
# VERSION=0.1.0
